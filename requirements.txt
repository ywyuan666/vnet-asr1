# ===== 耳机语音识别项目依赖 =====
# 建议 Python 3.9 ~ 3.11

# 深度学习框架（CPU 版即可跑通；有 NVIDIA 显卡可换 CUDA 版）
torch>=2.0.0
torchaudio>=2.0.0

# WeNet 本体不要从 pip 装：setup_autodl.sh / Dockerfile 会从官方源码安装，
# 这样训练入口和解码入口与本项目的 CLI/config 保持一致。

# 语音合成：自动生成训练用的语音指令
edge-tts>=6.1.0

# 音频读写 / 处理
soundfile>=0.12.1
numpy>=1.23
librosa>=0.10.0

# 麦克风实时识别 demo 用（infer_demo.py --mic）
sounddevice>=0.4.6

# Web Demo 服务（server/app.py）
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# 进度条 / 工具
tqdm>=4.65
pyyaml>=6.0

# WeNet 源码运行依赖（跳过 torch 后需要手动补齐）
requests
jieba
sentencepiece
langid
tensorboard
tensorboardX
Pillow
textgrid
openai-whisper
